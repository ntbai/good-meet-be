spring:
  application:
    name: good-meet
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/goodmeet}
    username: ${SPRING_DATASOURCE_USERNAME:goodmeet}
    password: ${SPRING_DATASOURCE_PASSWORD:goodmeet}
    hikari:
      connection-init-sql: SET TIME ZONE 'UTC'
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  jackson:
    time-zone: UTC
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: false
    serialization:
      write-dates-as-timestamps: false
  modulith:
    detection-strategy: explicitly-annotated

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

application:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  identity:
    tokens:
      issuer: good-meet-backend
      audience: good-meet-api
      access-token-ttl: 15m
      secret: ${PLATFORM_JWT_SECRET:dev-only-platform-secret-change-me}
  participant:
    guest-token:
      issuer: good-meet-backend
      audience: good-meet-guest
      token-ttl: 30m
      secret: ${GUEST_JWT_SECRET:dev-only-guest-secret-change-me-now}
  livekit:
    url: ${LIVEKIT_URL:ws://localhost:7880}
    api-key: ${LIVEKIT_API_KEY:devkey}
    api-secret: ${LIVEKIT_API_SECRET:dev-only-livekit-secret-change-me-now}
    token-ttl: 5m
    room-prefix: goodmeet-

logging:
  level:
    root: info
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn
